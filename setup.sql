-- PostgreSQL Database Setup Script
-- Run this script to create the database for Bank Management System

-- Create database (run this as postgres superuser)
CREATE DATABASE bankdb;

-- Create a user (optional, you can use postgres user)
-- CREATE USER bankuser WITH PASSWORD 'your_password';
-- GRANT ALL PRIVILEGES ON DATABASE bankdb TO bankuser;

-- Connect to bankdb and you're ready to run the application
-- The application will automatically create the required tables
